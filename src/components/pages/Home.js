import React, { Fragment } from 'react';
import Search from '../user/Search.component';
import Users from '../user/Users.component';

const Home = () => (
	<Fragment>
		<Search />
		<Users />
	</Fragment>
);

export default Home;
